Task
Implement Astacar logos per placement and specify asset locations.

Header logo = two SVGs:

Random icon (per page load) from:
1_front_astacar_logo.svg, 2_front_astacar_logo.svg, 3_front_astacar_logo.svg, 4_front_astacar_logo.svg, 5_front_astacar_logo.svg, 6_front_astacar_logo.svg, 7_front_astacar_logo.svg, 8_front_astacar_logo.svg, 9_front_astacar_logo.svg, 10_front_astacar_logo.svg, 11_front_astacar_logo.svg, 12_front_astacar_logo.svg.
Color: solid #4B0082.

Constant wordmark: astacar_logo_font.svg.
Fill: linear gradient left→right: #4B0082 → #32DCFA → #00FFAF.

Footer logo = one SVG:

astacar_logo_font.svg in solid white.

Asset locations (Replit project)

Place all SVG files in a single static assets directory that will be served in production:
Option A (bundled import): src/assets/logos/… (import into components for tree‑shaking and cache‑busting). [Recommended for React/Vite setups]
Option B (public/static): public/logos/… (refer by absolute URL “/logos/…”, not imported via JS). [Use if you need direct URL access or non‑bundled assets]

Choose one option and keep it consistent across header/footer implementations. Document which one you used in the PR/README.

Functional requirements

On each page load, randomly select one icon file from the 12 listed and render it left of the wordmark in the header.

Force the icon color to #4B0082:

Preferred: ensure the icon paths use fill="currentColor" and apply class text-[#4B0082].

If the source SVGs have hardcoded fills, replace them at build time (SVGO transform) or inline and set fill="#4B0082".

Header wordmark must render with a linearGradient fill:

0%: #4B0082, 50%: #32DCFA, 100%: #00FFAF.

Use inline SVG with <defs><linearGradient id="astacarGradient"> and fill="url(#astacarGradient)". Ensure unique IDs if multiple instances exist on the page.

Footer wordmark must render in solid white:

Set fill="white" or fill="currentColor" with class text-white.

Sizing and layout

Header: icon + wordmark aligned center vertically; gap 8–12px; suggested classes h-8 md:h-10 (w-auto).

Footer: wordmark h-8 md:h-10 (w-auto); align per site style (left/center).

Accessibility

Wrap logos in a link to home with aria-label="Astacar — home".

Provide <title>Astacar</title> within inline SVGs or aria-hidden="true" if redundant.

Implementation notes

Random pick (JS/TS):
const files = Array.from({ length: 12 }, (_, i) => ${i + 1}_front_astacar_logo.svg);
const pick = files[Math.floor(Math.random() * files.length)];
// If using src/assets: import via dynamic import or a manifest map.
// If using public: <img src={/logos/${pick}} …>

Gradient wordmark (inline SVG example):
<svg viewBox="0 0 W H" role="img" aria-label="Astacar wordmark" class="h-8 md:h-10 w-auto">
<defs>
<linearGradient id="astacarGradient" x1="0%" y1="0%" x2="100%" y2="0%">
<stop offset="0%" stop-color="#4B0082"/>
<stop offset="50%" stop-color="#32DCFA"/>
<stop offset="100%" stop-color="#00FFAF"/>
</linearGradient>
</defs>
<path d="…" fill="url(#astacarGradient)" />
</svg>

If using external SVGs for the wordmark, pre-bake the gradient inside astacar_logo_font.svg and reference it via <img>. Inline is preferred for full control.

Acceptance criteria

Header shows a random icon (from the 12 files) + gradient wordmark on every reload.

Icon color is exactly #4B0082; no leftover original fills.

Footer shows only the wordmark in solid white.

Assets are stored in the documented directory (src/assets/logos or public/logos) and load correctly in Replit static deployment.

Logos render sharply on retina and scale correctly across breakpoints.

Testing checklist

Reload to verify random selection across all 12 icons.

Verify gradient rendering in Chrome, Safari, Firefox, Edge.

Confirm correct asset paths work in both dev and deployed Replit environments.

Dark/light theme contrast is acceptable; no layout shift.